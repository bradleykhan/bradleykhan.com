import styled from "styled-components";

const Title = styled.h1`
    color: var(--white);
    font-family: "Inter";
    font-size: var(--biggest);
    font-weight: 600;
    margin-top: 20px;
    margin-bottom: 20px;
`;

export default Title;