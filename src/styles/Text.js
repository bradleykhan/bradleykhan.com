import styled from "styled-components";

const Text = styled.p`
    color: var(--lightgrey);
    font-family: "Inter";
    font-size: var(--medium);
    font-weight: 400;
    line-height: 24px;
`;

export default Text;